<Window x:Class="SATCalculator.Views.NewClauseWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SATCalculator.Views"
        mc:Ignorable="d"
        Title="New clause" Height="266" Width="386" WindowStartupLocation="CenterScreen">


    <DockPanel>
        <StatusBar DockPanel.Dock="Bottom" VerticalAlignment="Bottom">
            <TextBlock Text="{Binding Message}"/>
        </StatusBar>

        <WrapPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="Create" Click="CreateFormula" ToolTip="Create and add the clause in the formula"/>
            <Button Content="Cancel" Click="Cancel" ToolTip="Exit without adding a clause in the formula"/>
        </WrapPanel>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>

            <ListBox Grid.Row="0">
                
            </ListBox>
        </Grid>

    </DockPanel>
</Window>

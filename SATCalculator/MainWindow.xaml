<Window x:Class="SATCalculator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SATCalculator"
        mc:Ignorable="d"
        Title="MainWindow" Height="604" Width="977" WindowStartupLocation="CenterScreen">

    <DockPanel>
        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="Load formula" Click="LoadFormula"/>
            <Button Content="Analyze" Click="AnalyzeFormula"/>
        </StackPanel>

        <ScrollViewer DockPanel.Dock="Top" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Hidden">
            <TextBox Background="Transparent" BorderThickness="0" Text="{Binding Formula.DisplayValue, Mode=OneWay}" 
                     IsReadOnly="True" TextWrapping="NoWrap" />
        </ScrollViewer>

        <Grid>
            <TabControl>
                <TabItem Header="Formula">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="auto"/>
                            <ColumnDefinition Width="auto"/>
                            <ColumnDefinition Width="auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Grid Grid.Column="0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0">
                                <Run Text="clauses: "/>
                                <Run Text="{Binding Formula.ClausesCount, Mode=OneWay}"/>
                            </TextBlock>
                            <!--<ListBox Grid.Row="1" ItemsSource="{Binding Formula.Clauses}"/>-->
                            <ListView Grid.Row="1" ItemsSource="{Binding Formula.Clauses}">
                                <ListView.View>
                                    <GridView>
                                        <GridViewColumn Header="Clause" Width="120" DisplayMemberBinding="{Binding Name}" />
                                        <GridViewColumn Header="Trinity" Width="120" DisplayMemberBinding="{Binding Trinity.Contents}" />
                                    </GridView>

                                </ListView.View>
                            </ListView>
                        </Grid>

                        <Grid Grid.Column="1">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0">
                                <Run Text="variables: "/>
                                <Run Text="{Binding Formula.VariablesCount, Mode=OneWay}"/>
                            </TextBlock>
                            <ListView Grid.Row="1" ItemsSource="{Binding Formula.Variables}">
                                <ListView.View>
                                    <GridView>
                                        <!--<GridViewColumn Header="Key" DisplayMemberBinding="{Binding Key}" />-->
                                        <GridViewColumn Header="Name" Width="90" DisplayMemberBinding="{Binding Value.Name}" />
                                        <GridViewColumn Header="References" Width="90" DisplayMemberBinding="{Binding Value.ReferencesInLiterals}" />
                                    </GridView>

                                </ListView.View>
                            </ListView>
                        </Grid>

                        <Grid Grid.Column="2">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0">
                                <Run Text="trinities: "/>
                                <Run Text="{Binding Formula.TrinitiesCount, Mode=OneWay}"/>
                            </TextBlock>
                            <ListView Grid.Row="1" ItemsSource="{Binding Formula.Trinities}">
                                <ListView.View>
                                    <GridView>
                                        <GridViewColumn Header="Name" Width="90" DisplayMemberBinding="{Binding Value.Contents}" />
                                        <GridViewColumn Header="References" Width="90" DisplayMemberBinding="{Binding Value.References}" />
                                    </GridView>

                                </ListView.View>
                            </ListView>
                        </Grid>

                    </Grid>
                </TabItem>
            </TabControl>
        </Grid>

    </DockPanel>
</Window>

<Window x:Class="SATCalculator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SATCalculator"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" WindowStartupLocation="CenterScreen">
    
    <DockPanel>
        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="Analyze" Click="AnalyzeFormula"/>
        </StackPanel>

        <StackPanel Orientation="Vertical">
            <TextBlock Text="{Binding FormulaString, Mode=OneWay}"/>
            <ListBox ItemsSource="{Binding AnalysisResults}"/>
            <TabControl>
                <TabItem Header="Formula"/>
            </TabControl>
        </StackPanel>

    </DockPanel>
</Window>
